name: Test PAW3222 helpers

on:
  push:
  pull_request:

jobs:
  helpers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test delta decoding and pointer acceleration
        run: make test
